name: gentrail-m3-local

services:

  postgres:
    image: docker.io/library/postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gentrail}
      POSTGRES_USER: ${POSTGRES_USER:-gentrail}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gentrail}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gentrail} -d ${POSTGRES_DB:-gentrail}"]
      interval: 2s
      timeout: 2s
      retries: 20

  dynamodb-local:
    image: docker.io/amazon/dynamodb-local:2.5.2
    command: -jar DynamoDBLocal.jar -sharedDb -inMemory
    ports:
      - "8000:8000"

  nats:
    image: docker.io/library/nats:2.10-alpine
    command: ["-js", "-sd", "/data", "-m", "8222"]
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - nats-data:/data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 2s
      timeout: 2s
      retries: 20

  init-aws:
    image: docker.io/amazon/aws-cli:2.17.0
    depends_on:
      dynamodb-local: { condition: service_started }
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-local}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-local}
      AWS_REGION: ${AWS_REGION:-us-west-2}
      DDB_ENDPOINT: http://dynamodb-local:8000
    entrypoint: ["/bin/sh", "/init/init-aws.sh"]
    volumes:
      - ./init:/init:ro
    restart: "no"

  dashboard:
    image: ${GENTRAIL_IMAGE_REPO:-public.ecr.aws/p6a2v1k1}/dashboard:${GENTRAIL_IMAGE_TAG:-v0.3.0}
    depends_on:
      postgres: { condition: service_healthy }
      init-aws: { condition: service_completed_successfully }
      nats: { condition: service_healthy }
    environment:
      NATS_URL: nats://nats:4222
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-local}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-local}
      AWS_REGION: ${AWS_REGION:-us-west-2}
      AWS_ENDPOINT_URL_DYNAMODB: http://dynamodb-local:8000
      DATABASE_URL: postgres://${POSTGRES_USER:-gentrail}:${POSTGRES_PASSWORD:-gentrail}@postgres:5432/${POSTGRES_DB:-gentrail}?sslmode=disable
      DYNAMO_TRACES_TABLE: gentrail-traces
      DYNAMO_VIOLATIONS_TABLE: gentrail-violations
      DYNAMO_AGENT_STATS_TABLE: gentrail-agent-stats
      DYNAMO_POLICY_RULES_TABLE: gentrail-policy-rules
      DYNAMO_API_KEYS_TABLE: gentrail-api-keys
      DYNAMO_WINDOW_MARKERS_TABLE: gentrail-window-markers
      DYNAMO_HEARTBEATS_TABLE: gentrail-heartbeats
      LICENSE_JWT: ${LICENSE_JWT:-}
    ports:
      - "8001:8001"

  policy-engine:
    image: ${GENTRAIL_IMAGE_REPO:-public.ecr.aws/p6a2v1k1}/policy-engine:${GENTRAIL_IMAGE_TAG:-v0.3.0}
    environment:
      LICENSE_JWT: ${LICENSE_JWT:-}
    ports:
      - "8100:8100"

  evaluator:
    image: ${GENTRAIL_IMAGE_REPO:-public.ecr.aws/p6a2v1k1}/evaluator:${GENTRAIL_IMAGE_TAG:-v0.3.0}
    depends_on:
      init-aws: { condition: service_completed_successfully }
      nats: { condition: service_healthy }
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-local}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-local}
      AWS_REGION: ${AWS_REGION:-us-west-2}
      AWS_ENDPOINT_URL_DYNAMODB: http://dynamodb-local:8000
      DYNAMO_TRACES_TABLE: gentrail-traces
      DYNAMO_VIOLATIONS_TABLE: gentrail-violations
      DYNAMO_AGENT_STATS_TABLE: gentrail-agent-stats
      DYNAMO_POLICY_RULES_TABLE: gentrail-policy-rules
      DYNAMO_WINDOW_MARKERS_TABLE: gentrail-window-markers
      EVENT_BUS: nats
      NATS_URL: nats://nats:4222
      LICENSE_JWT: ${LICENSE_JWT:-}

  # OTLP ingest. In the cloud this is a Lambda Function URL publishing to SNS;
  # here it is a long-running HTTP server (EVENT_BUS=nats) publishing
  # trace.ingested to JetStream, completing the local ingest -> eval path.
  authproxy:
    image: ${GENTRAIL_IMAGE_REPO:-public.ecr.aws/p6a2v1k1}/authproxy:${GENTRAIL_IMAGE_TAG:-v0.3.0}
    depends_on:
      init-aws: { condition: service_completed_successfully }
      nats: { condition: service_healthy }
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-local}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-local}
      AWS_REGION: ${AWS_REGION:-us-west-2}
      AWS_ENDPOINT_URL_DYNAMODB: http://dynamodb-local:8000
      DYNAMO_TRACES_TABLE: gentrail-traces
      DYNAMO_AGENT_STATS_TABLE: gentrail-agent-stats
      DYNAMO_API_KEYS_TABLE: gentrail-api-keys
      EVENT_BUS: nats
      NATS_URL: nats://nats:4222
      PORT: "4318"
      LICENSE_JWT: ${LICENSE_JWT:-}
    ports:
      - "4318:4318"

volumes:
  nats-data:

