apiVersion: v1
kind: ServiceAccount
metadata:
  name: evaluator
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "gentrail.labels" (merge (dict "component" "evaluator") .) | indent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.irsa.evaluator | quote }}
