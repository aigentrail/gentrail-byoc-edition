apiVersion: v1
kind: ServiceAccount
metadata:
  name: authproxy
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "gentrail.labels" (merge (dict "component" "authproxy") .) | indent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.irsa.authproxy | quote }}
